name: SmallVoice
options:
  bundleIdPrefix: io.github.notime2
  deploymentTarget:
    macOS: "26.0"
  createIntermediateGroups: true
  developmentLanguage: en
configFiles:
  Debug: Config/Signing.xcconfig
  Release: Config/Signing.xcconfig
settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    ENABLE_HARDENED_RUNTIME: YES
    DEAD_CODE_STRIPPING: YES
    LOCALIZATION_PREFERS_STRING_CATALOGS: YES
    SWIFT_EMIT_LOC_STRINGS: YES
packages:
  ParakeetKit:
    path: Packages/ParakeetKit
  KeyboardShortcuts:
    url: https://github.com/sindresorhus/KeyboardShortcuts
    exactVersion: 3.1.0
targets:
  SmallVoice:
    type: application
    platform: macOS
    sources:
      - path: SmallVoice
    dependencies:
      - package: ParakeetKit
      - package: KeyboardShortcuts
    info:
      path: SmallVoice/Resources/Info.plist
      properties:
        CFBundleDisplayName: SmallVoice
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        LSUIElement: true
        LSApplicationCategoryType: public.app-category.productivity
        NSMicrophoneUsageDescription: "SmallVoice listens only while you dictate and turns your speech into text right on this Mac."
        NSHumanReadableCopyright: "Speech model: Parakeet Redux by Moondream, based on NVIDIA Parakeet TDT 0.6B v3 (CC-BY-4.0)."
    entitlements:
      path: SmallVoice/Resources/SmallVoice.entitlements
      properties:
        com.apple.security.device.audio-input: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(SMALLVOICE_BUNDLE_ID)
        PRODUCT_NAME: SmallVoice
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_APP_SANDBOX: NO
schemes:
  SmallVoice:
    build:
      targets:
        SmallVoice: all
    run:
      config: Debug
    profile:
      config: Release
    archive:
      config: Release
